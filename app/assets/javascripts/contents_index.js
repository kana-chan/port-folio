$(function(){
  if(window.location.href.match(/\//)){
// ================================================
    $.when(
      $('.top').fadeIn(3000)
    ).done(function(){

    })

// ================================================
  }
});