#categoryForm
  = form_with model: @category, local: true do |f|
    .skill
      .skill--name
        %p スキル
        = f.text_field :name,placeholder:"スキル名"
      .skill--text
        %p 説明
        = f.text_area :text,placeholder:"テキスト"
      .skill--number
        %p 番号
        = f.number_field :number,placeholder:"半角数字"
      .skill--icon
        %p アイコン
        = f.text_field :icon, placeholder:"svgファイル"
    .submit
      = f.submit
    -if @category.persisted?
      =link_to(category_path(@category.id),method: :delete, class: "deleteBtn", data: { confirm:"本当に削除しますか?"}) do
        削除する